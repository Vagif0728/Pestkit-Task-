@model List<Department>
@{
    int count = 0;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Departments table</h4>
            <a asp-controller="Department" asp-action="Create" class="btn btn-warning fw-bold fs-6 text-white">Create Department</a>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                Image
                            </th>
                            <th>
                                Name
                            </th>
                            <th>
                                Employees Count
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Department department in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    <img src="~/img//@department.ImgUrl" />
                                </td>
                                <td>
                                    @department.Name
                                </td>
                                <td>
                                    @department.Employees.Count
                                </td>
                                <td>
                                    <a asp-action="Update" asp-controller="Department" asp-route-id="@department.Id" class="btn btn-primary btn-info">
                                        <i class="mdi mdi-pencil text-white"></i>
                                    </a>
                                    <a asp-action="Delete" asp-controller="Department" asp-route-id="@department.Id" class="btn btn-danger btn-info">
                                        <i class="mdi mdi-delete text-white"></i>
                                    </a>
                                    <a asp-action="More" asp-controller="Department" asp-route-id="@department.Id" class="btn btn-primary">
                                        <i class="mdi mdi-open-in-new text-white"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>