@model List<Slide>
@{
    int count = 0;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Slides table</h4>
            <a asp-controller="Slide" asp-action="Create" class="btn btn-warning fw-bold fs-6 text-white">Create Slide</a>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                Image
                            </th>
                            <th>
                                Title
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Slide slide in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    <img src="~/img//@slide.Photo.FirstOrDefault()?.Url" />
                                </td>
                                <td>
                                    @slide.Title
                                </td>
                                <td>
                                    <a asp-action="Update" asp-controller="Blog" asp-route-id="@slide.Id" class="btn btn-primary btn-info">
                                        <i class="mdi mdi-pencil"></i>
                                    </a>
                                    <a asp-action="Delete" asp-controller="Blog" asp-route-id="@slide.Id" class="btn btn-danger btn-info">
                                        <i class="mdi mdi-delete"></i>
                                    </a>
                                    <a asp-action="More" asp-controller="Blog" asp-route-id="@slide.Id" class="btn btn-primary">
                                        <i class="mdi mdi-open-in-new"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>