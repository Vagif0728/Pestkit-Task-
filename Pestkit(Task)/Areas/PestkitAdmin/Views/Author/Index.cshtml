@model List<Author>
@{
    int count = 0;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Authors table</h4>
            <a asp-controller="Author" asp-action="Create" class="btn btn-warning fw-bold fs-6 text-white">Create Author</a>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                Full Name
                            </th>
                            <th>
                                Blogs Count
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Author author in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    @author.Name @author.Surname
                                </td>
                                <td>
                                    @author.Blogs.Count
                                </td>
                                <td>
                                    <a asp-action="Update" asp-controller="Author" asp-route-id="@author.Id" class="btn btn-primary btn-info">
                                        <i class="mdi mdi-pencil text-white"></i>
                                    </a>
                                    <a asp-action="Delete" asp-controller="Author" asp-route-id="@author.Id" class="btn btn-danger btn-info">
                                        <i class="mdi mdi-delete text-white"></i>
                                    </a>
                                    <a asp-action="More" asp-controller="Author" asp-route-id="@author.Id" class="btn btn-primary">
                                        <i class="mdi mdi-open-in-new text-white"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>