@model List<Tag>
@{
    int count = 0;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Tags table</h4>
            <a asp-controller="Tag" asp-action="Create" class="btn btn-warning fw-bold fs-6 text-white">Create Tag</a>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                Title
                            </th>
                            <th>
                                Blogs Count
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Tag tag in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    @tag.Name
                                </td>
                                <td>
                                    @tag.BlogTags.Count
                                </td>
                                <td>
                                    <a asp-action="Update" asp-controller="Tag" asp-route-id="@tag.Id" class="btn btn-primary btn-info">
                                        <i class="mdi mdi-pencil text-white"></i>
                                    </a>
                                    <a asp-action="Delete" asp-controller="Tag" asp-route-id="@tag.Id" class="btn btn-danger btn-info">
                                        <i class="mdi mdi-delete text-white"></i>
                                    </a>
                                    <a asp-action="More" asp-controller="Tag" asp-route-id="@tag.Id" class="btn btn-primary">
                                        <i class="mdi mdi-open-in-new text-white"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>